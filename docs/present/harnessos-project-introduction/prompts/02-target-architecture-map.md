# 02 Target Architecture Map

Use `shared-style.md`.

```text
Create a 16:9 target architecture relationship map for HarnessOS. Left side:
current accepted stack with V12 read-only workbench, V13 editable Studio pilot,
V14 extension governance, V15 observability baseline, PV16 runtime hardening
pilot. Right side: target architecture planes with Product Console, Mission
Studio, BFF and DTO boundary, Product Entity Control, Workflow DSL, Agent
Station Config, Plugin Skill Tool MCP, Runtime Gateway, Evidence, Observability
and Deployment. Use arrows showing additive gated evolution from current to
target. Highlight browser never directly writes runtime truth. Keep short
Chinese labels only.
```
