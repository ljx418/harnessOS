# tool_permission_blocks

A realistic terminal UI mockup showing governed tool execution blocks: tool started, command preview, approval prompt, allow/deny keys, sandbox mode, forbidden reason, evidence ref. Chinese labels. Minimal developer CLI style, no marketing cockpit, no browser dashboard.
