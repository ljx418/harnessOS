{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://harnessos.local/schemas/v15_observability_deployment_artifact_manifest.schema.json",
  "title": "V15ObservabilityDeploymentArtifactManifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "stage_id",
    "status",
    "evidence_scope",
    "not_production_ga_evidence",
    "required_artifacts",
    "blocked_claims",
    "created_at"
  ],
  "properties": {
    "schema_version": { "const": "v15.observability_deployment_artifact_manifest.v1" },
    "stage_id": { "enum": ["V15-R0", "V15-S1", "V15-S2", "V15-S3", "V15-SA"] },
    "status": { "enum": ["PLANNED_NOT_EVIDENCE", "DRAFT", "PASS", "FAIL", "BLOCKED"] },
    "evidence_scope": {
      "enum": [
        "readiness",
        "observability_dashboard",
        "deployment_smoke",
        "final_scenario_matrix",
        "aggregate_reconciliation"
      ]
    },
    "not_production_ga_evidence": { "type": "boolean" },
    "required_artifacts": {
      "type": "array",
      "minItems": 20,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["artifact_id", "path", "required_for_pass", "status"],
        "properties": {
          "artifact_id": { "type": "string", "minLength": 1 },
          "path": { "type": "string", "minLength": 1 },
          "required_for_pass": { "type": "boolean" },
          "status": { "enum": ["PLANNED", "PRESENT", "MISSING", "NOT_APPLICABLE"] },
          "sha256": { "type": ["string", "null"] }
        }
      }
    },
    "blocked_claims": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "created_at": { "type": "string", "format": "date-time" }
  }
}
